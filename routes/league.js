const express = require("express")
const router = express.Router();

router.get("/", (req,res)=>{})
router.get("/:league_id", (req,res)=>{})
router.post("/", (req,res)=>{})
router.put("/:league_id", (req,res)=>{})
router.delete("/:league_id", (req,res)=>{})

module.exports = router;
